import React from 'react';
import {Center, ICenterProps} from 'native-base';

/**
 *@description Center width 100% view
 */
function Center100View(props: ICenterProps) {
  return <Center w="100%" {...props} />;
}

export default Center100View;
